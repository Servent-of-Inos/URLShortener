<b-modal ref= "create" :ok-only ="true" id="create" title="Specify some information:">

    <b-form @submit.prevent="sendLongUrl" @reset="onReset" autocomplete="off">
    
	    <b-form-group id="UrlInputGroup"
		                label="Url:"
		                label-for="UrlInput"
		                description="Enter url you want to shorten">
		    <b-form-input id="UrlInput"
		                type="url"
		                v-model="newUrl.long_url"
		                required
		                placeholder="example-site.com">
		    </b-form-input>
	    </b-form-group>

	    <b-form-group id="LifetimeInputGroup"
		                label="Lifetime of your url:"
		                label-for="lifetime"
		                description="This field is optional, if empty your short link will work permanently">
		               
		    <datepicker 
		    	name = "lifetime" v-model="newUrl.lifetime" confirm
		    	lang="en" type="datetime" format="YYYY-MM-DD HH:mm">
		    </datepicker>

	    </b-form-group>

	    <b-form-checkbox-group v-model="newUrl.is_active" id="ActiveCheck">

	        <b-form-checkbox>Check if you want activete your short link after creation</b-form-checkbox>
	        
        </b-form-checkbox-group>

	    <b-button type="submit" variant="primary btn-sm">Submit</b-button>
	    <b-button type="reset" variant="danger btn-sm">Reset</b-button>

    </b-form>

</b-modal>